===========================
EpicManager - a general purpose bukkit plugin.

This plugin implements multiple simple commands:

/ban
/unban
/kick
/sethome
/home
/rmhome


INSTALATION
 This project is most easily built with maven.

 Type the following in the project directory:

 >  mvn clean install

 Maven downloads dependencies and builds the jar.  The jar will be in the
 /target directory.
